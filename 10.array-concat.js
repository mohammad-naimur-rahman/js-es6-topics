var numbers1 = [1, 2, 3, 4, 5];
var numbers2 = [6, 7, 8, 9, 10];

var result = numbers1.concat(numbers2);

console.log(result); // concat() joins arrays[2 or more] togather
console.log(numbers1); // and doesn't change the main array
console.log(numbers2);